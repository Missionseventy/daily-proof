// js/app.js
// Keep this file lightweight.
// Pricing links are now hardcoded in pricing.html to avoid mismatch bugs.

document.addEventListener("DOMContentLoaded", () => {
  // If later you want Day/Night toggle globally, we can wire it here
  // without touching page styling again.
});




